# SOLUTION: Port Mismatch Fixed
# Changes: Set targetPort to 8001 to match container port

apiVersion: v1
kind: Service
metadata:
  name: log-processor
  namespace: log-processor
spec:
  selector:
    app: log-processor
  ports:
  - port: 80
    # FIXED: Matches container port
    targetPort: 8001
    protocol: TCP
  type: ClusterIP
