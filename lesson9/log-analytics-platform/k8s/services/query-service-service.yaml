apiVersion: v1
kind: Service
metadata:
  name: query-service
  namespace: log-analytics
  labels:
    app: query-service
    service: query-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: query-service
