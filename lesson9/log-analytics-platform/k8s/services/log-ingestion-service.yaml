apiVersion: v1
kind: Service
metadata:
  name: log-ingestion
  namespace: log-analytics
  labels:
    app: log-ingestion
    service: log-ingestion
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: log-ingestion
