# FIXED VERSION of Scenario 2
apiVersion: v1
kind: ResourceQuota
metadata:
  name: storage-quota
  namespace: scenario-02
spec:
  hard:
    requests.storage: "20Gi"  # FIXED: Increased to allow 15Gi + buffer
    persistentvolumeclaims: "5"  # FIXED: Allow 3 replicas + buffer
