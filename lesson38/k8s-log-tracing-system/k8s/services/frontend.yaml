apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: app
spec:
  type: LoadBalancer              # expose externally; use NodePort for kind/minikube
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: http
