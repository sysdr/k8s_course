apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: log-analytics-gateway
  namespace: log-analytics
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "logs.example.com"
    tls:
      httpsRedirect: true
  - port:
      number: 443
      name: https
      protocol: HTTPS
    tls:
      mode: SIMPLE
      credentialName: logs-tls-cert
    hosts:
    - "logs.example.com"
