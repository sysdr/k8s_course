apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: secrets-platform-gateway
  namespace: secrets-platform
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: frontend-vs
  namespace: secrets-platform
spec:
  hosts:
  - "*"
  gateways:
  - secrets-platform-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: frontend
        port:
          number: 80
