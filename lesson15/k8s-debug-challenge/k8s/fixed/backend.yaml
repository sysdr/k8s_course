# FIXED VERSION - Service name matches frontend expectation
apiVersion: v1
kind: Service
metadata:
  name: api-backend
  namespace: debug-challenge
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: backend
