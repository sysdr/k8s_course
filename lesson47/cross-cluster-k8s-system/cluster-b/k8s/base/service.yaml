apiVersion: v1
kind: Service
metadata:
  name: log-processor
  namespace: logging
  labels:
    app: log-processor
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: log-processor
