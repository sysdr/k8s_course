apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/part-of: argocd
data:
  # Default policy
  policy.default: role:readonly
  
  # RBAC policies
  policy.csv: |
    # Admin role - full access
    p, role:admin, applications, *, */*, allow
    p, role:admin, clusters, *, *, allow
    p, role:admin, repositories, *, *, allow
    p, role:admin, projects, *, *, allow
    
    # Developer role - read applications, sync own apps
    p, role:developer, applications, get, */*, allow
    p, role:developer, applications, sync, */*, allow
    p, role:developer, applications, list, *, allow
    p, role:developer, projects, get, *, allow
    
    # Readonly role - view only
    p, role:readonly, applications, get, */*, allow
    p, role:readonly, applications, list, *, allow
    p, role:readonly, projects, get, *, allow
    
    # Group mappings (update with your SSO groups)
    g, platform-team, role:admin
    g, backend-team, role:developer
    g, external-viewers, role:readonly
