Thank you for installing {{ .Chart.Name }}!

Your log processing system has been deployed to the {{ .Values.global.namespace }} namespace.

To check the status of your deployment:

  kubectl get pods -n {{ .Values.global.namespace }}

To get the dashboard URL:

  {{- if eq .Values.dashboard.service.type "LoadBalancer" }}
  kubectl get svc dashboard-service -n {{ .Values.global.namespace }}
  {{- else }}
  kubectl port-forward svc/dashboard-service 8080:80 -n {{ .Values.global.namespace }}
  Then visit: http://localhost:8080
  {{- end }}

To access Grafana dashboards:

  kubectl port-forward svc/grafana 3000:3000 -n {{ .Values.global.namespace }}
  Then visit: http://localhost:3000 (admin/{{ .Values.monitoring.grafana.adminPassword }})

To test the ingestion API:

  kubectl port-forward svc/ingestion-service 8000:8000 -n {{ .Values.global.namespace }}
  
  curl -X POST http://localhost:8000/api/v1/ingest \
    -H "Content-Type: application/json" \
    -d '{"level":"INFO","service":"test","message":"Hello from Kubernetes!"}'

Key Pod Patterns Demonstrated:
  ✓ Init Containers (Analytics Engine)
  ✓ Sidecar Pattern (Dashboard with Nginx)
  ✓ Health Probes (All services)
  ✓ Resource Management (Requests & Limits)
  ✓ HPA & PDB (Ingestion API)

For more information, visit: https://github.com/k8s-odyssey/log-processing
