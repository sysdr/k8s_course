apiVersion: v1
kind: Service
metadata:
  name: analytics-api
  namespace: log-platform
  labels:
    app: analytics-api
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: analytics-api
